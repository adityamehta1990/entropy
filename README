Python based portfolio analysis module for Indian mutual fund investments.

Built using Flask + MongoDB and pandas (obviously). Built as proof of concept, no longer maintained.

With a mongod instance, run
    > python server.py
in the commandline. 

URLs:
    1. For nav of a fund: /fund-data/nav/<schemeCode>
    2. For schemes: /fund-data/schemes or /fund-data/schemes/<navDate>
    3. For return fo a fund: /fund-data/return/<schemeCode>/<window>
